server {
    listen 8082;
    server_name _;

    root /var/www/fatystyle;
    index index.html;

    # Recette sur IP : ne jamais indexer. Les ressources peuvent être conservées
    # localement mais sont revalidées à chaque navigation pour rester à jour.
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), geolocation=(), microphone=()" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Cache-Control "no-cache, must-revalidate" always;

    gzip on;
    gzip_vary on;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;

    include /etc/nginx/snippets/fatystyle-public-contact.conf;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~* \.(jpg|jpeg|png|gif|webp|avif|svg|ico|css|js|woff|woff2|ttf|json)$ {
        expires off;
        try_files $uri =404;
    }

    location ~ \.php$ {
        return 404;
    }

    location ~ /\. {
        deny all;
    }
}
